---

tasks:
  - tags: 
      - lolcat
      - cli
    cargo:
      package: lcat
      state: ${{ package.state }}

  - tags: 
      - lolcat
      - cli
    symlinks:
      src: ~/.cargo/bin/lcat.exe
      dest: ~/.local/bin/lolcat.exe
      state: ${{ package.state }}
