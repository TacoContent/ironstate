---

tasks:
  - name: Install Python
    tags:
      - languages
      - languages.python
    winget:
      package: "${{ languages.python | toggle(defaults.languages.python) }}"
      state: "${{ languages.python | ternary('present', 'absent') }}"
