---

tasks:
  - name: Install JetBrains Aqua Editor
    when: (development.editors is mapping or (development.editors | bool)) and (development.editors.aqua | default(false))
    winget:
      package: JetBrains.Aqua
      state: "${{ package.state | default('present')}}"
      source: winget