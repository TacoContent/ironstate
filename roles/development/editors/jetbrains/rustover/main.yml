---

tasks:
  - name: Install JetBrains RustOver Editor
    when: (development.editors is mapping or (development.editors | bool)) and (development.editors.rustover | default(false))
    winget:
      package: JetBrains.RustOver
      state: "${{ package.state | default('present')}}"
      source: winget