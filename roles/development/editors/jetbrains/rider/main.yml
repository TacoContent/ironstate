---

tasks:
  - name: Install JetBrains Rider
    when: (development.editors is mapping or (development.editors | bool)) and (development.editors.rider | default(false))
    winget:
      package: JetBrains.Rider
      state: "${{ package.state | default('present')}}"
      source: winget