---

tasks:
  - name: Install JetBrains ReSharper
    when: (development.editors is mapping or (development.editors | bool)) and (development.editors.resharper | default(false))
    winget:
      package: JetBrains.ReSharper
      state: "${{ package.state | default('present')}}"
      source: winget