---

tasks:
  - name: Install JetBrains WebStorm Editor
    when: (development.editors is mapping or (development.editors | bool)) and (development.editors.webstorm | default(false))
    winget:
      package: JetBrains.WebStorm
      state: "${{ package.state | default('present')}}"
      source: winget