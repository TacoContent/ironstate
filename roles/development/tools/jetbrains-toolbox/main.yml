---

tasks:
  - name: Install JetBrains Toolbox
    when: (development.editors is mapping or (development.editors | bool)) and (development.editors.jetbrains_toolbox | default(false))
    winget:
      package: JetBrains.Toolbox
      state: "${{ package.state | default('present')}}"
      source: winget