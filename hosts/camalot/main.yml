---

tasks:

  - name: Install Package Managers
    include:
      name: roles/package-managers
      state: latest

  - name: Install Languages
    when: (languages is mapping) or (languages is boolean and languages)
    include:
      name: roles/languages
      state: present
    tags:
      - languages

  - name: Set up Development Environment
    include:
      name: roles/development
      state: present
    tags:
      - development

  - name: Set up Shell Environment
    when: (shell is mapping) or (shell is boolean and shell)
    include:
      name: roles/shell
      state: present
    tags:
      - shell

  - name: Set up SSH Environment
    when: (ssh is mapping) or (ssh is boolean and ssh)
    include:
      name: roles/ssh
      state: present
    tags:
      - ssh

  - name: Include Productivity Role
    when: productivity | enabled()
    include:
      name: roles/productivity
      state: present
    tags:
      - productivity

  - name: Include Gaming Role
    when: gaming | enabled()
    include:
      name: roles/gaming
      state: present
    tags:
      - gaming

  - name: System Tools
    when: system | enabled()
    include:
      name: roles/system
      state: present
    tags:
      - system

  - name: Install Windows Terminal
    when: terminal | default(true)
    include:
      name: roles/terminal
      state: present
    tags:
      - terminal

  - name: Install Security Tools
    when: security | enabled()
    include:
      name: roles/security
      state: present
    tags:
      - security

  - name: Install CLI Tools
    include:
      name: packages/cli
      state: present
    when: (cli is mapping) or (cli is boolean and cli)
    tags:
      - cli


  - name: register local bins
    path:
      paths:
        - ${{ facts.local_bin_path }}
        - ~/bin
      state: present

  # Includes: define a package once under packages/<name>/main.yml (using
  # the same 'tasks:' shape as this file) and pull it in with 'include:'.
  # See README.md's "Includes" section for the full mechanism, including
  # 'src'/'script' path resolution and '${{ ... }}' template expressions.
  - name: install lolcat
    tags: 
      - lolcat
      - cli
    include:
      name: packages/lolcat
      state: present
